import 'package:flutter/material.dart';

class ActiveNavBarItem extends StatelessWidget {
  const ActiveNavBarItem({super.key});
  // final String icon;
  @override
  Widget build(BuildContext context) {
    return SizedBox();
    // return SvgPicture.asset(icon);
  }
}
